[build]
  # 메인 배포 폴더만 지정합니다 (가장 안정적인 예전 방식)
  publish = "jerrykeyring_legacy"
  command = ""

[build.processing]
  # 리다이렉트 간섭 방지를 위해 최적화 프로세싱 건너뜀
  skip_processing = true

[[redirects]]
  # /nfc/4 주소를 /nfc/4.html로 연결
  from = "/nfc/*"
  to = "/nfc/:splat.html"
  status = 200
